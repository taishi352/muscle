<div class="jumbotron">
	<h1 class="text-center">投稿詳細画面</h1>
</div>

<div class="container">

	<% if flash[:notice] %>
      <p><%= flash[:notice] %></p>
    <% end %>

	<div class="row">

		<div class="col-md-2">
			<p class="text-center"><%= attachment_image_tag(@post.user, :profile_image, :fill, 90, 100, fallback: "no_image.jpg", class: "img-circle") %></p>
			<h4 class="text-center"><%= @post.user.name %></h4>
			<% if @post.user.id == current_user.id %>
				<p class="text-center"><%= link_to "Edit",edit_post_path(@post.id), class: "btn-sm btn-success" %>
				<%= link_to "Destroy",post_path, method: :delete, data: {confirm: "本当に削除してもよろしいですか？"}, class: "btn-sm btn-danger" %></p>
			<% end %>
		</div>

		<div class="col-md-7">
			<h2 class="text-center">「<%= @post.title %>」</h2>
			<div class="text-right">
				<p>ジャンル：<%= @post.genre.genre_name %></p>
				<% if @post.favorited_by?(current_user) %>
					<%= link_to post_favorites_path(@post), method: :delete do %>
						<span class="glyphicon glyphicon-heart" aria-hidden="true" style="color: red;"></span>
						<%= @post.favorites.count %>
					<% end %>
				<% else %>
					<%= link_to post_favorites_path(@post), method: :post do %>
						<span class="glyphicon glyphicon-heart" aria-hidden="true"></span>
						<%= @post.favorites.count %>
					<% end %>
				<% end %>
			</div>
			<p>本文：<br><%= @post.body %></p>
		</div>

		<div class="col-md-3">
			<span class="text-center"><%= attachment_image_tag(@post, :post_image, :fill, 250, 200) %></span>
		</div>

	</div>

	<p class="text-right">コメント件数:<%= @post.post_comments.count %>件</p>
	<table class="table">
		<% @post.post_comments.each do |post_comment| %>
		<tr>
			<td><%= post_comment.user.name %></td>
			<td><%= post_comment.comment %></td>
			<td>
				<% if @post.user.id == current_user.id %>
				<%= link_to "Destroy",post_post_comments_path(@post), method: :delete, class: "btn-sm btn-danger" %>
				<% end %>
			</td>
		</tr>
		<% end %>
	</table>
	<!-- コメント入力フォーム -->
	<%= form_for [@post, @post_comment] do |f| %>
		<%= f.text_area :comment, rows:'3', class: "form-control" %>
		<%= f.submit "送信" %>
	<% end %>
</div>